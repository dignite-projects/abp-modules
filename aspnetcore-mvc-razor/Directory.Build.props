<Project>

  <!-- MSBuild imports only the NEAREST Directory.Build.props per project and stops walking up, so
       this file must explicitly import the repo root's to inherit the lockstep <Version> /
       <AssemblyVersion> / shared metadata. -->
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />

  <PropertyGroup>
    <Product>Dignite Abp AspNetCore Mvc Razor</Product>
    <PackageTags>abp;aspnetcore;mvc;razor;multi-tenant-views</PackageTags>
  </PropertyGroup>

  <!-- Each tree keeps its OWN README.md as the packed PackageReadmeFile, deliberately: the root
       README.md is a monorepo landing page, and packing it into every package would put a sibling
       tree's documentation on this package's NuGet page. CONTRIBUTING/SECURITY/CHANGELOG/LICENSE
       are repo-wide and live only at the root. -->
  <ItemGroup>
    <None Include="$(MSBuildThisFileDirectory)README.md" Pack="true" PackagePath="\" Visible="false" />
    <None Include="$(MSBuildThisFileDirectory).github\icon.png" Pack="true" PackagePath="\" Visible="false" />
  </ItemGroup>

</Project>
