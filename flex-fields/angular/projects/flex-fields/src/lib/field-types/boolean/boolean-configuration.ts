/**
 * Configuration of a `Boolean` field, shaped for `FormBuilder.group()`. Mirrors `BooleanConfiguration`
 * on the server.
 */
export class BooleanConfiguration {
  'Boolean.Default': unknown = [false];
}
