/**
 * How a text field renders its input. Mirrors the `TextMode` enum on the server — the ordinals are
 * the stored value of `Text.Mode`.
 */
export enum TextMode {
  SingleLine = 0,
  MultipleLine = 1,
}
