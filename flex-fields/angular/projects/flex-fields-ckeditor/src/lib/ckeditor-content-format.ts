/**
 * Output format a CKEditor field's value is persisted in. Mirrors the `CKEditorContentFormat` enum on
 * the server — the ordinals are the stored value of `CKEditor.ContentFormat`.
 */
export enum CKEditorContentFormat {
  Html = 0,
  Markdown = 1,
}
